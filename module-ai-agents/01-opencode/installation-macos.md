# Установка OpenCode на macOS

Цель: установить OpenCode на macOS и проверить, что он запускается из терминала.

## Что понадобится

- macOS;
- установленный терминал;
- Node.js;
- npm;
- доступ в интернет;
- права на установку пользовательских пакетов.

Ссылки:

- [OpenCode Intro](https://opencode.ai/docs/)
- [Node.js Downloads](https://nodejs.org/en/download)
- [npm Documentation](https://docs.npmjs.com/)
- [OpenCode Download](https://opencode.ai/download)

## Шаг 1. Установите Node.js

OpenCode можно ставить через npm, поэтому сначала нужен Node.js.

Проверьте, установлен ли Node.js:

```bash
node -v
```

Ожидаемый результат: терминал выводит версию Node.js.

Проверьте npm:

```bash
npm -v
```

Ожидаемый результат: терминал выводит версию npm.

Если команды не найдены, установите Node.js с официального сайта:

```text
Скачайте установщик с официальной страницы Node.js
```

## Шаг 2. Установите OpenCode через npm

Команда установки:

```bash
npm install -g opencode-ai
```

Ожидаемый результат: npm устанавливает пакет без ошибок.

Зачем так:

- команда повторяет официальный путь из документации;
- установка через npm дает простой и повторяемый сценарий;
- это минимально зависимый способ без дополнительных менеджеров пакетов.

## Шаг 3. Проверьте запуск

После установки проверьте, что бинарник доступен:

```bash
opencode --help
```

Ожидаемый результат: OpenCode выводит справку по командам и параметрам.

## Шаг 4. Первый запуск

Запустите OpenCode в каталоге проекта или отдельной рабочей папки:

```bash
opencode
```

Ожидаемый результат: OpenCode запускается в интерактивном режиме.

Если OpenCode требует выбора модели или авторизации, это нормально: на этом этапе вы проверяете, что сам инструмент стартует.

## Шаг 5. Обновление

Если вы ставили OpenCode через npm, обновление обычно выполняется той же глобальной установкой:

```bash
npm install -g opencode-ai
```

Ожидаемый результат: npm обновляет установленный пакет или сообщает, что актуальная версия уже установлена.

Это перезапишет установленную версию на новую, если пакет обновился.

## Альтернатива через Homebrew

Если в вашей среде принято использовать Homebrew, официальная документация OpenCode также показывает установку через tap:

```bash
brew install anomalyco/tap/opencode
```

Ожидаемый результат: Homebrew устанавливает OpenCode без ошибок.

Такой вариант подходит, если Homebrew у вас уже используется как основной пакетный менеджер.

## Критерий завершения

Установка завершена, если `node -v`, `npm -v` и `opencode --help` выводят результат без ошибок.
