# Модели в OpenCode

Одна из типичных ошибок новичков заключается в том, что они воспринимают агент как отдельный продукт, а модель как что-то вторичное. На практике все наоборот: поведение агента зависит от того, какую модель вы подключили, как она умеет работать с инструментами и сколько стоит ее использование.

## Что важно понять сразу

OpenCode поддерживает множество провайдеров и локальные модели. Это означает, что сам агент не привязан только к одному API.

Официальные источники:

- [OpenCode Models](https://opencode.ai/docs/models/)
- [OpenCode Providers](https://opencode.ai/docs/providers/)
- [OpenCode Download](https://opencode.ai/download)

## Какие модели можно использовать

В OpenCode можно использовать:

- облачные модели от популярных провайдеров;
- локальные модели, если они доступны в вашей среде;
- модели, подключенные через поддерживаемого провайдера;
- модели, доступные через конфигурацию OpenCode.

Смысл здесь не в том, чтобы запомнить длинный список названий. Смысл в том, чтобы понимать критерии выбора:

- качество рассуждения;
- качество tool calling;
- скорость;
- цена;
- доступность в вашей организации;
- совместимость с вашей задачей.

## Какие модели бесплатные

Строго говоря, «бесплатная» модель в агентном сценарии бывает двух типов:

- локальная модель, если вы запускаете ее у себя;
- модель, доступная через провайдера с бесплатным планом или trial.

Важно различать:

- сам OpenCode может быть бесплатным как инструмент;
- использование облачной модели может стоить денег;
- локальная модель может не требовать API-ключа, но потребует вычислительных ресурсов.

Если смотреть на OpenCode Zen, официальная документация отдельно перечисляет бесплатные модели, доступные на момент публикации страницы. В документации встречаются, например:

- `Big Pickle`;
- `GLM 4.7 Free`;
- `Kimi K2.5 Free`;
- `MiniMax M2.1 Free`.

Важно: OpenCode указывает, что эти бесплатные варианты доступны ограниченное время. Поэтому в учебных материалах лучше ссылаться на официальный Zen-документ и не закреплять их как вечную гарантию.

## Какие модели требуют API-ключ

Если модель работает через облачного провайдера, почти всегда нужен API-ключ. В OpenCode ключи обычно подключаются через команду `/connect` или через переменные окружения в конфиге.

Пример из документации OpenCode:

```json
{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "openai": {
      "options": {
        "apiKey": "{env:OPENAI_API_KEY}"
      }
    }
  }
}
```

## Ограничения бесплатных моделей

Нельзя рассчитывать, что бесплатная модель всегда будет одинаково полезна для агентных задач. Ограничения обычно бывают такими:

- ограничение по количеству запросов;
- ограничение по скорости;
- ограничения на tool calling;
- меньшая стабильность при длинных цепочках действий;
- меньшее окно контекста;
- возможные ограничения по качеству ответа на сложные задачи.

Для Threat Intelligence это важно, потому что задача часто включает:

- навигацию по большим страницам;
- извлечение шаблонов;
- повторяющиеся проверки;
- обогащение данных;
- сохранение результата.

Если модель плохо работает с инструментами, пайплайн быстро становится ненадежным.

## Где получить API-ключи

Получайте ключи только из официальных кабинетов провайдера.

Типовой сценарий:

1. Зайдите в кабинет провайдера.
2. Создайте API key.
3. Сохраните его в переменной окружения.
4. Подключите его в конфиг OpenCode.

Примеры официальных стартовых страниц:

- [OpenAI API docs](https://platform.openai.com/docs)
- [Anthropic docs](https://docs.anthropic.com/)
- [Google AI documentation](https://ai.google.dev/)
- [OpenCode Zen](https://opencode.ai/docs/zen/)

## Как выбирать модель для учебных задач

Для учебного модуля выбирайте модель, которая:

- хорошо работает с инструментами;
- не слишком дорогая для ваших тестов;
- доступна в вашей среде;
- умеет последовательно выполнять действия.

Практически это означает, что для практики важнее надежность выполнения сценария, чем «максимально умный» ответ в абстрактном смысле.

## Практические команды

Посмотреть список доступных моделей:

```bash
opencode /models
```

Проверить выбранную модель в конфиге:

```bash
cat ~/.config/opencode/opencode.json
```

Проверить переменную окружения:

```bash
echo "$OPENAI_API_KEY"
```

## TODO: screenshot

TODO: screenshot

## Что должно получиться

- вы понимаете разницу между локальными и облачными моделями;
- вы знаете, что «бесплатно» не всегда означает «без ограничений»;
- вы можете объяснить, почему для агентной задачи важен tool calling;
- вы знаете, где получать ключи у официального провайдера.

## Проверка результата

- откройте `/models` в OpenCode;
- проверьте, что выбранная модель появляется в списке;
- проверьте наличие API-ключа в переменных окружения или конфиге;
- убедитесь, что вы используете официальные кабинеты провайдеров.
